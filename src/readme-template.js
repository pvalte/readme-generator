module.exports = readmeData => {
    return `
    good job paigey
    ${readmeData.title}
    ${readmeData.description}
    ${readmeData.instructions}
    ${readmeData.usage}
    ${readmeData.contribution}
    ${readmeData.test}
    ${readmeData.license}
    ${readmeData.username}
    ${readmeData.email}
    `;
};